*.tar.gz filter=lfs diff=lfs merge=lfs -text

# Force LF line endings for shell scripts and Docker configs
# Prevents CRLF corruption when checked out on Windows and copied into Linux containers
*.sh text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf
*.conf text eol=lf
xstartup text eol=lf
